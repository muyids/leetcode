班上有 N 名学生。其中有些人是朋友，有些则不是。他们的友谊具有是传递性。如果已知 A 是 B 的朋友，B 是 C 的朋友，那么我们可以认为 A 也是 C 的朋友。所谓的朋友圈，是指所有朋友的集合。

给定一个 N * N 的矩阵 M，表示班级中学生之间的朋友关系。如果M[i][j] = 1，表示已知第 i 个和 j 个学生互为朋友关系，否则为不知道。你必须输出所有学生中的已知的朋友圈总数。

```case
示例 1:

输入:
[[1,1,0],
 [1,1,0],
 [0,0,1]]
输出: 2
说明：已知学生0和学生1互为朋友，他们在一个朋友圈。
第2个学生自己在一个朋友圈。所以返回2
```

---

### 解题思路

并查集求解

### 基本代码

```cpp
class Solution {
public:
    int findCircleNum(vector<vector<int>> &M) {
        int n = M.size();
        vector<int> p(n, 0);
        for (int i = 0; i < n; i++) {
            p[i] = i;
        }
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                if (M[i][j] && i != j) {
                    unionSet(p, i, j);
                }
            }
        }
        int ans = 0;
        for (int i = 0; i < n; i++) {
            if (i == find(p, i)) { // 统计集合数
                ans++;
            }
        }
        return ans;
    }

    int find(vector<int> &p, int x) {
        if (p[x] != x) {
            p[x] = find(p, p[x]);
        }
        return p[x];
    }

    void unionSet(vector<int> &p, int x, int y) {
        int px = find(p, x);
        int py = find(p, y);
        if (px != py) {
            p[py] = px;
        }
    }
};
```

### 按秩合并

使具有较少节点的树的根指向具有较多节点的树的根

维护一个秩，表示该节点高度的一个上界，在按秩合并策略的union操作中，让具有较少的秩的根指向具有较大秩的根。

```cpp
class Solution {
public:
    int findCircleNum(vector<vector<int>> &M) {
        int n = M.size();
        vector<int> p(n, 0);
        for (int i = 0; i < n; i++) {
            p[i] = i;
        }
        vector<int> rank(n, 0);
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                if (M[i][j] && i != j) {
                    unionSet(p,rank, i, j);
                }
            }
        }
        int ans = 0;
        for (int i = 0; i < n; i++) {
            if (i == find(p, i)) {
                ans++;
            }
        }
        return ans;
    }

    int find(vector<int> &p, int x) {
        if (p[x] != x) {
            p[x] = find(p, p[x]);
        }
        return p[x];
    }

    void unionSet(vector<int> &p, vector<int> &rank,int x, int y) {
        x = find(p, x);
        y = find(p, y);
        if (rank[x] < rank[y]) p[x]= y;
        else {
            p[y] = x;
            if (rank[x] == rank[y]) rank[x]++;
        }
    }
};
```

### 路径压缩

使查找路径中的每个节点直接指向根

### 继续优化

初始朋友圈的数量为n, 每合并一次，减少一个朋友圈

```cpp
class Solution {
public:
    int findCircleNum(vector<vector<int>> &M) {
        int n = M.size();
        int ans = n;
        vector<int> p(n, 0);
        for (int i = 0; i < n; i++) {
            p[i] = i;
        }
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                if (M[i][j] && i != j) {
                    int x = find(p, i);
                    int y = find(p, j);
                    if (x != y) {
                        ans--;
                        p[y] = x;
                    }
                }
            }
        }
        return ans;
    }

    int find(vector<int> &p, int x) {
        if (p[x] != x) {
            p[x] = find(p, p[x]);
        }
        return p[x];
    }
};
```
